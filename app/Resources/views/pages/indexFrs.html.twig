<!DOCTYPE html>
<html lang="en">
{{include('pages/inc/header.html.twig')}}
{% if is_granted('IS_AUTHENTICATED_FULLY')%}

<h3>Afficher les achats</h3>
<button onclick="location.href='{{absolute_url(asset(''))}}app_dev.php/createFournisseur'" type="button" class="btn btn-dark">Ajouter un fournisseur</button>
<button onclick="location.href='{{absolute_url(asset(''))}}app_dev.php/login_success'" type="button" class="btn btn-outline-dark">la page d'accueil</button>

  <table class="table">
    <thead>
      <tr>
        <th>Déscription d'un fournisseur</th>
        <th>Entreprise</th>
        <th>mail</th>
        <th>telephone</th>
        <th>Fax</th>
      </tr>
    </thead>
     <!--[if expression]>
     <![endif]--> 
 
     	<tbody>
     	{% for fournisseur in fournisseurs %}
      <tr>
        <td>{{fournisseur.entreprise}}</td>
        <td>{{ fournisseur.mail}}</td>
        <td>{{ fournisseur.telephone}}</td>
        <td>{{ fournisseur.fAX}}</td>

         <td>
      <button onclick="location.href='{{absolute_url(asset(''))}}app_dev.php/viewFrs/{{fournisseur.id}}'" type="button" class="btn btn-outline-dark">Afficher</button>
   
 </td>
    
       <td> <button onclick="location.href='{{absolute_url(asset(''))}}app_dev.php/editFrs/{{fournisseur.id}}'" type="button" class="btn btn-default"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></button></td>

        <td> <button onclick="location.href='{{absolute_url(asset(''))}}app_dev.php/deleteFrs/{{fournisseur.id}}'" type="button" class="btn btn-danger"><span class="glyphicon glyphicon-trash" aria-hidden="true"></span></button></td>


      </tr>      
      {% else %}
      <tr>
      	<td>Aucun resultat à afficher </td>
       </tr>
      {% endfor %}
     
    </tbody>
     

  </table>
  <a href="{{logout_path()}}" class="btn btn-danger">se deconnecter</a>
  {% endif %}
  {{include('pages/inc/footer.html.twig')}}